// This is included in the AppContext.jsx artifact
export const initialState = {
  activeTab: 'home',
  customText: '',
  // ... rest from artifact
};

export const appReducer = (state, action) => {
  // ... implementation from artifact
};